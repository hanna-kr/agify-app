import 'package:flutter/material.dart';

class MyColor {
  static const Color primary = Color.fromRGBO(102, 33, 98, 1);

  static const Color secondary = Color.fromRGBO(108, 203, 209, 1);

  static const Color tealLight = Color.fromRGBO(228, 247, 248, 1);

  static const Color primaryDark = Color.fromRGBO(51, 32, 60, 1);

  static const Color white = Color.fromRGBO(255, 255, 255, 1);
}
