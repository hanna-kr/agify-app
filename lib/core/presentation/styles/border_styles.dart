import 'package:flutter/material.dart';

class MyBorderRadius {
  static const BorderRadius m = BorderRadius.all(Radius.circular(10.0));
  static const BorderRadius l = BorderRadius.all(Radius.circular(20.0));
}
